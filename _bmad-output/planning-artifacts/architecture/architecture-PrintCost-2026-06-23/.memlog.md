---
scope: PrintCost MVP architecture from PRD and UX spines
purpose: build-substrate
altitude: feature
updated: 2026-06-23T17:14
---

- (direction) User chose Coaching path for architecture creation.
- (direction) Additional human-facing artifact needed: implementation handoff for builder agents.
- (version) Verified npm current versions on 2026-06-23: @angular/core 22.0.2, @angular/cli 22.0.3, @angular/router/forms/service-worker 22.0.2, TypeScript 6.0.3, idb 8.0.3, lucide-angular 1.0.0, sass 1.101.0.
zy-loaded feature routes, German UI with English implementation identifiers.
- (constraint) Source UX binds mobile-first PWA with four primary German surfaces: Kalkulation, Bestand, Filamente, Mehr; compact single-column tool surface; bottom nav; German visible copy and accessibility labels; DESIGN.md tokens are visual contract.
- (constraint) Source PRD binds Angular 18+ standalone components, strict TypeScript, Angular Signals, Typed Reactive Forms, SCSS/CSS custom properties, IndexedDB/idb, Angular service worker, static hosting, lazy-loaded feature routes, German UI with English implementation identifiers.
- (decision) Adopt current Angular 22 PWA seed: Angular 22 standalone components/routes, strict TypeScript, Angular Signals, Typed Reactive Forms, SCSS/CSS custom properties, idb 8.0.3, Angular service worker, Lucide Angular 1.0.0; binds foundation and all feature epics; prevents builders mixing framework generations, state libraries, or persistence wrappers.
- (decision) Adopt feature-sliced layered frontend paradigm: lazy feature routes own screens and form orchestration; shared domain services own IndexedDB access, mutation rules, snapshots, backup, settings, and calculation engine; binds all epics; prevents route components directly mutating storage or duplicating domain rules.
- (decision) Adopt service-owned state mutation: feature services expose readonly Signals and async command methods; all IndexedDB writes and referential behavior flow through services; pure calculation function has no storage dependency; binds printer, filament, calculation, inventory, customer, settings, template, and backup features; prevents inconsistent mutation paths and formula side effects.
- (decision) Filament stock must deduct when an item is printed, not remain manual-only; binds calculation inventory and filament inventory; prevents saved/printed inventory drifting from filament remainingG.
- (decision) User corrected calculation/stock semantics: gramsUsed is total material for the whole print job even when plates > 1; material cost must not multiply gramsUsed by plates; saving a Calculation does not mean printed and must not deduct filament; deduction occurs only when user records a print occurrence such as +1/Druck verbuchen.
- (decision) User corrected plate semantics: plate count does not multiply material or time-based costs; printMinutes and gramsUsed are total job inputs; plates only expose an optional percent extra-work fee/surcharge. Saved calculations live in Bestand > Drucke, not Teile; Teile is for manual parts only.
- (decision) Backup JSON is canonical external data contract; import validates full object and schema version before clearing data; import replace writes all stores atomically where IndexedDB permits and must fail before mutation if validation fails; merge import deferred; future schema changes require IndexedDB migrations and backup version bumps; binds backup, settings, all stores; prevents partial destructive imports and version drift.
- (decision) Adopt source/module seed: app shell owns bottom nav/update banner; core/db owns idb connection schema migrations; core/backup owns import/export validation; core/locale owns de-DE formatters; domain/models owns English interfaces/enums; domain/calculation owns pure formula and tests; domain/validation owns sanitizers; features own route screens; shared/ui owns reusable controls. Feature screens may call services but not idb directly; backup reaches stores through core/db; domain calculation has no Angular dependency; binds all epics; prevents feature-local persistence and formula duplication.
- (decision) Adopt GitHub Pages as default static hosting seed for MVP; hosted app has no backend; CSP denies network connections; Angular service worker precaches app shell/local assets only; update banner is non-blocking and user-controlled; CI should run build/tests and budget/Lighthouse checks where practical; binds foundation and operations; prevents backend/cloud assumptions and hidden user-data transfer.
- (decision) Because plate count no longer multiplies costs, implementation and backup v1 should name the percentage field extraWorkFeePercent rather than multiPlateSurchargePercent; binds models, forms, calculation, backup; prevents old PRD field name from carrying wrong semantics.
- (decision) Print occurrence deduction uses the saved filamentId even if the filament is soft-deleted; if current remainingG is lower than required grams, do not block the print occurrence because stock is approximate, clamp remainingG to 0 and show a German warning; if the filament record is missing, block the deduction/print command with a German data error.
- (event) spine finalized
